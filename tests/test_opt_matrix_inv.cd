int main() {
    // Matrix with determinant -1 (integer inverse)
    matrix a[2][2] = {{1,2},{3,5}};
    matrix inva[2][2];

    inva = a.inv;

    print("inv:", inva[0][0], inva[0][1], inva[1][0], inva[1][1]);
    return 0;
}
